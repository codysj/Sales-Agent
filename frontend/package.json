{
  "name": "matrix-power-dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Internal review dashboard for the Matrix Power sales agent. Not published.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "generate:api": "openapi-typescript openapi.json -o lib/api-types.ts"
  },
  "//overrides": "T-152. next@16.2.12 is the latest stable and pins postcss to 8.4.31 and sharp to ^0.34.5, both of which carry high-severity advisories; the fix exists only in next's preview/canary line, and npm audit fix proposes next@9.3.3 — a five-major downgrade, which is worse than the advisories. These raise the two transitive packages instead, within their own major. Drop an entry once a stable next ships past it.",
  "overrides": {
    "postcss": "^8.5.25",
    "sharp": "^0.35.3"
  },
  "dependencies": {
    "next": "^16.2.12",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "jsdom": "^30.0.1",
    "openapi-typescript": "^7.13.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  }
}
